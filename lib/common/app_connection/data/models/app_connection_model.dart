import 'package:flutter/material.dart';

import '../../domain/entities/app_connection.dart';

class AppConnectionModel extends AppConnection {
  AppConnectionModel({@required String baseUri}) : super(baseUri: baseUri);
}
